# Normalize line endings to LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Treat binary assets as binary.
*.webp binary
*.png  binary
*.avif binary
*.jpg  binary
*.jpeg binary
*.ico  binary
